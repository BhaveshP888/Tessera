{
  "name": "tessera-monorepo",
  "private": true,
  "type": "module",
  "packageManager": "bun@1.3.14",
  "workspaces": [
    "packages/*",
    "apps/*",
    "server",
    "extensions/*"
  ],
  "scripts": {
    "build": "turbo run build && bun -e \"import fs from 'node:fs'; if (fs.existsSync('apps/web/dist')) fs.cpSync('apps/web/dist', 'dist', { recursive: true });\"",
    "vercel-build": "turbo run build && bun -e \"import fs from 'node:fs'; if (fs.existsSync('apps/web/dist')) fs.cpSync('apps/web/dist', 'dist', { recursive: true });\"",
    "dev": "turbo run dev",
    "test": "bun test",
    "typecheck": "turbo run typecheck"
  },
  "devDependencies": {
    "turbo": "^2.4.4",
    "typescript": "^5.7.3",
    "@types/node": "^22.13.4"
  }
}
